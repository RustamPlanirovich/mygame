import { defineConfig } from 'vitest/config';

/**
 * Отдельный конфиг, а не `test` внутри vite.config.ts: сборочный конфиг тянет react-плагин,
 * manualChunks и dev-proxy, которые тестам не нужны и только замедляют запуск.
 *
 * Тесты покрывают ЧИСТЫЕ функции: форматирование чисел, резолвер подписей,
 * сериализацию/патч сохранений, расчёты кредитов и очередь стройки. Компоненты и сторы
 * сюда намеренно не тянутся — им нужен jsdom и моки, а основные регрессии (bigplan.md:
 * пункты 6, 9, 14) ловятся именно на чистом слое.
 */
export default defineConfig({
  test: {
    environment: 'node',
    include: ['src/**/*.test.{ts,tsx}', 'server/**/*.test.js'],
  },
});
