# VIEW — UX/UI план (как должна выглядеть игра)

Дата: 24.12.2025

Цель этого документа: описать целевой вид интерфейса (1 экран), чтобы игра была удобна в ежедневном использовании в жанре idle/factory: быстро “увидел узкое место → выбрал клетку → починил поток → продолжил прогресс”.

Ограничения (важно):
- Используем только существующие токены темы и подходы проекта (Tailwind + классы `.cyber-panel`, `.cyber-button`).
- Не вводим новые цвета/шрифты — опираемся на `cyber-*` из `tailwind.config.js`.
- Иконки — только из `lucide-react`.

---

## 1) Дизайн-система (строгие правила)

### 1.1 Цвета (только текущие токены)
- Фон приложения: `bg-cyber-black`.
- Панели/контейнеры: `bg-cyber-dark`, границы `border-cyber-gray`.
- Основной текст: `text-cyber-text`.
- Заголовки секций / ключевой акцент: `text-cyber-green`.
- Акцент “инфо/активно/выбрано”: `text-cyber-blue`.
- Ошибка/дефицит/опасность: `text-cyber-red`.

Рекомендованные уровни текста:
- Основной: `text-cyber-text`.
- Вторичный: `text-gray-400/500/600`.
- Третичный (подсказки): `text-gray-700`.

### 1.2 Типографика
- Числа и параметры: `font-mono`.
- Заголовки панелей: `text-xl`, `uppercase`, `tracking-wider`.
- Подписи/хинты: `text-xs`.

### 1.3 Компоненты-атомы
- Панель: `.cyber-panel` как базовый контейнер.
- Кнопка: `.cyber-button` как базовая CTA.
- Состояния кнопок:
  - недоступно: `disabled:opacity-50 disabled:cursor-not-allowed` (уже в `.cyber-button`).

---

## 2) Глобальная раскладка (1 экран)

### 2.1 Верхняя часть (всегда видима)
1) **Header (высота ~64px, sticky)**
- Слева: название проекта `PROTOCOL: YGGDRASIL` (как сейчас), цвет `text-cyber-green`.
- Справа: строка статуса `SYSTEM STATUS: ONLINE` (или фактический статус), `text-xs text-gray-500`.

2) **ResourceBar (sticky под хедером)**
- Липкая полоса ресурсов — всегда на экране.
- Формат карточки ресурса (chip):
  - Иконка (20px) + название (жирное) + строка `amount / max` (моно) + `+production/с`.
- Цветовая кодировка по смыслу:
  - Энергия: `text-cyber-green`.
  - Производственные ресурсы (руда/лёд/углерод/сталь/тёмная материя): `text-cyber-blue`.
- Хинт-поведение (правило): если `amount >= max` → добавлять рядом “(ПОЛНО)” в `text-cyber-red` или подсветку значения в `text-cyber-red`.

### 2.2 Основная область (2 колонки на desktop)
- **Слева**: FactoryGrid (сетку нельзя “терять”).
- **Справа**: Sidebar (скролл), где игрок решает проблему выбранной клетки.

Респонсив:
- На узких экранах: сетка сверху, сайдбар снизу (как сейчас: `grid-cols-1`).

---

## 3) Левая область: FactoryGrid (сетка)

### 3.1 Визуальные правила клеток
- Пустая клетка: заливка `cyberGray` с низкой альфой.
- Занятая клетка: заливка `cyberBlue` с низкой альфой.
- База: заливка `cyberGreen` с низкой альфой + текст “БАЗА”.
- Обводка:
  - Нормальная: `cyberGray`.
  - Дефицит входов (локально нет нужного ресурса): `cyberRed`.

### 3.2 Надписи внутри клетки
- Короткий бейдж здания (3 буквы): из `BUILDING_BADGE`.
- При дефиците входов: строка `НЕТ: РУД,ЛЁД,...` красным.

### 3.3 Линии (маршруты)
- Линия должна быть визуально читаема на фоне.
- UX-хинты (обязательно отражать текстом где-то в сайдбаре):
  - Click: выбрать клетку.
  - Shift+Click по линии: выбрать линию (даже в режиме строительства).
  - Right Click по линии: удалить линию.
  - Delete/Backspace: удалить выбранную линию.

---

## 4) Правая область: Sidebar (панели)

Главный принцип: **сверху всегда “Инспектор”** (решает 80% действий). Остальные панели ниже — “контекст и прогресс”.

### 4.1 Общий стиль панелей
- Каждая панель:
  - Заголовок: `text-xl text-cyber-green uppercase tracking-wider`.
  - Справа в заголовке: короткий статус `text-xs text-gray-500`.
  - Контент внутри: `.cyber-panel` для ключевых блоков.
- Внутри карточек:
  - Название сущности: `text-cyber-blue font-bold`.
  - Описание: `text-xs text-gray-500`.
  - Числа/стоимости: `font-mono` + `text-gray-300`.

---

## 5) Панель “Инспектор” (UX-ядро)

Иконка заголовка: `Search` (lucide).

### 5.1 Состояния
- Нет выбранной клетки:
  - Текст: “Выбери клетку на сетке слева.” (`text-sm text-gray-600`).
- Выбрана База:
  - Блок “Склад базы”: список ресурсов + `amount / max`.
  - Блок “Прогресс”: кубиты, произведено энергии, итоговые множители.
  - Блок “Оборона”: турели/щит/давление.
  - Блок “Входящие линии”: перечисление.
- Выбрана клетка со зданием:
  - Заголовок: иконка здания (из `getBuildingIcon`) + название + “Всего установлено”.
  - Primary action справа: `СНЕСТИ` (кнопка `.cyber-button`).

### 5.2 Режим строительства (build mode)
- Строка “Режим строительства: …”
  - Если выбран билд: показывать иконку здания + название `text-gray-300`.
  - Кнопка `ОТМЕНА` справа.

### 5.3 I/O блок (входы/выходы)
- Входы и выходы показываются списком:
  - Название ресурса: `text-gray-300`.
  - Скорость: `font-mono`.
  - Источник/цель: “буфер/база” (`text-gray-500`).
- Правило подсветки причины простоя:
  - Если вход отсутствует → строка входа в `text-cyber-red` + текст “нет ресурса”.
  - Если выход заблокирован переполнением → строка выхода в `text-cyber-red` + “выход переполнен”.

### 5.4 Линии (links) и контекст выбранной линии
- Если линия выбрана:
  - Блок “Контекст выбранной линии”:
    - “Ресурс: … (A→B)”
    - “В источнике: …”
    - “В цели: …” (+ пометка “(база)” когда нужно)
    - “За последний тик ушло: …”
  - В этом блоке обязательно показывать: производит/потребляет ли выбранный ресурс (чтобы объяснять ошибки логистики).

### 5.5 Тексты кнопок инспектора
- “ВЫБРАТЬ” (в карточках зданий) / “ВЫБРАНО”
- “СНЕСТИ”
- “ОТМЕНА”
- “ОТМЕНИТЬ СОЕДИНЕНИЕ” (когда идёт linking)

---

## 6) Панель “Угроза” (Combat)

Иконка заголовка: `ShieldAlert`.

### 6.1 Верхняя строка
- “Статус: ТИШИНА/АТАКА/ОФФЛАЙН” — значение `text-gray-300`, подписи `text-gray-500`.
- Таймеры “до следующей волны / до конца волны”.

### 6.2 Ключевые показатели
- Целостность базы: прогресс-бар `bg-cyber-green`.
- Щит (если есть): прогресс-бар `bg-cyber-blue`.
- Дефицит энергии для обороны: подсвечивать `text-cyber-blue` (как “недокорм”), а критические состояния — `text-cyber-red`.

### 6.3 Нано-Рой
- Слайдеры с подписью эмодзи как есть:
  - “🔴 Атака”, “🟢 Ремонт”, “🔵 Буст”.
- Справа от каждого: “% · боты”.
- Ниже суммарно: урон/ремонт/множитель — значения `text-gray-300`.

### 6.4 Протокол «Эгида»
- Заголовок блока: `text-gray-400`.
- Покупка апгрейдов — карточки как в Research (единый паттерн).

---

## 7) Панель “Обмен” (Market)

Иконка заголовка: `ArrowLeftRight`.

### 7.1 Заголовок
- Справа: “Обновление через …с · Событие: … · Маржа: x…”
- В событиях: значение `text-gray-300`.

### 7.2 Карточка ресурса
- Название: `text-cyber-blue font-bold`.
- “Цена” и “С маржой” — вторичный текст.
- Кнопки:
  - `Продать 1`, `Продать 10`, `Продать 100`, `Продать всё`.

---

## 8) Панель “Исследования”

Иконка заголовка: `Microscope`.

### 8.1 Шапка
- Справа: “5 веток · уровни сохраняются” (`text-xs text-gray-500`).
- Отдельной строкой: “Чертежи: …” (значение `text-gray-300`).

### 8.2 Карточка апгрейда
- Название: `text-cyber-blue font-bold`.
- Описание: `text-xs text-gray-500`.
- Строка “Уровень: X / max · Эффект: …”.
- Кнопка: “КУПИТЬ” / “МАКС”.

### 8.3 Блок “Производственная матрица”
- Разделитель сверху (`border-t border-cyber-gray/40`).
- Прямо в карточке показывать “Сейчас: …” (hint) — `text-gray-700`.
- Цена: “N чертеж.”

---

## 9) Панель “Демоны”

Иконка заголовка: `Skull` (или `Ghost`, если хотим менее агрессивно).

Правила:
- Каждого демона показывать как карточку:
  - Название `text-cyber-blue`.
  - Цена (⚡/с) `font-mono`.
  - Статус “активен/не активен/не оплачен” вторичным текстом.
- Для активных — лёгкий визуальный маркер: обводка `border-cyber-green`.

---

## 10) Панель “Престиж”

Иконка заголовка: `RotateCcw`.

### 10.1 Верх
- Кубиты и произведённая энергия за цикл.
- Кнопка: “ПРЕСТИЖ: СБРОС” (широкая).
- Подкнопочный текст:
  - Если доступно: “Получить: +N кубит(ов)”.
  - Если нет: “Недостаточно произведённой энергии”.

### 10.2 Квантовая нейросеть
- Блок как в Research:
  - Тратит кубиты.
  - Карточки апгрейдов.
  - Hint:
    - Chrono Shift: показывать стартовый пакет ресурсов.
    - Memory Preservation: “Активно/Не активно”.
- Select “Сохранить здание…”:
  - Disabled пока Memory Preservation не активно.
  - Подсказка маленьким текстом.

---

## 11) Панель “Экспедиции”

Иконка заголовка: `Rocket`.

Правила:
- Должна иметь “ситуационную” строку статуса:
  - “в полёте / возвращается / готово”
  - таймер.
- Награды (в том числе чертежи) показывать компактно, но всегда понятно.
- Кнопки должны быть в стиле `.cyber-button` и иметь короткие глаголы.

---

## 12) Панель “Инфраструктура” (список зданий)

Иконка заголовка: `Factory` (или `Building2`).

Правила карточки здания (как сейчас, это удачный паттерн):
- Слева:
  - Иконка здания (из `getBuildingIcon`) + название `text-cyber-blue`.
  - Уровень “Ур. N” `text-gray-500`.
  - Описание `text-gray-400`.
  - Производство/потребление/бой/защита — `text-xs`.
- Справа:
  - CTA “ВЫБРАТЬ/ВЫБРАНО”.
  - Стоимость в `text-xs`.
- Визуальные состояния:
  - Выбранное здание: обводка `border-cyber-green`.
  - Недоступно по ресурсам: disabled.

---

## 13) Иконки (единый словарь)

### 13.1 Ресурсы (уже используются)
- Энергия: `Zap`
- Руда: `Box`
- Лёд: `Snowflake`
- Углерод: `Atom`
- Сталь: `Layers`
- Тёмная Материя: `Sparkles`

### 13.2 Заголовки панелей (рекомендуемые)
- Инспектор: `Search`
- Угроза: `ShieldAlert`
- Обмен: `ArrowLeftRight`
- Исследования: `Microscope`
- Демоны: `Skull` / `Ghost`
- Престиж: `RotateCcw`
- Экспедиции: `Rocket`
- Инфраструктура: `Factory` / `Building2`

### 13.3 Иконки действий (рекомендуемые)
- Снести: `Trash2`
- Отмена/сброс выбора: `X`
- Соединить/маршрут: `Link2`
- Удалить линию: `Unlink`
- Подсказки/инфо: `Info`

---

## 14) Микрокопирайтинг (короткие правила текста)
- Заголовки секций — 1 слово или 1–2 слова: “Инспектор”, “Обмен”, “Исследования”, “Престиж”.
- Кнопки — глагол в повелительном: “Купить”, “Выбрать”, “Снести”, “Отмена”.
- Причины простоя формулировать всегда одинаково:
  - “Нет входа: …”
  - “Выход переполнен: …”
  - “Нет энергии”

---

## 15) Критерии удобства (чеклист)
- Любая проблема на фабрике должна диагностироваться за ≤10 секунд через: сетка (красные клетки) → инспектор (I/O + причина).
- Ресурсы и их скорость всегда доступны в верхней липкой панели.
- Каждая панель в сайдбаре использует один и тот же паттерн: заголовок → статус → карточки `.cyber-panel` → CTA `.cyber-button`.
